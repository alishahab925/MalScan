# Samples

This directory contains test samples for MalScan.

## EICAR Test File
`eicar_test.bin` contains the standard EICAR antivirus test string. It can be used to verify the analyzer's ability to process files and extract strings.

**Note**: Most antivirus software will flag this file as a threat, which is its intended purpose. It is completely harmless and contains only ASCII characters.
